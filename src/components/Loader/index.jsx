import React from 'react';

import { Container, LoaderIcon } from './styles';

const Loader = () => {
    return (
        <Container>
            <LoaderIcon />
        </Container>
    );
};

export default Loader;